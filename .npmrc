# pnpm 11 runs a dependency status check before every script and hard-fails on
# un-built dependencies (core-js/esbuild). CI installs explicitly and build
# approval is handled via onlyBuiltDependencies, so skip that pre-script re-check.
verify-deps-before-run=false
