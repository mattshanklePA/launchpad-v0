One-line: Radio option — group several by a shared `name`.

```jsx
<Radio name="cadence" value="q" label="Quarterly" checked={c==='q'} onChange={()=>setC('q')} />
<Radio name="cadence" value="m" label="Monthly"   checked={c==='m'} onChange={()=>setC('m')} />
```
