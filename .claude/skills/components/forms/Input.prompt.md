One-line: Labelled text field — mono uppercase label, limestone border, active-blue focus ring.

```jsx
<Input label="Spend gate" prefix="$" suffix="USD" placeholder="0" />
<Input label="Owner" error="This field is required" />
```

Supports `prefix` / `suffix` nodes, `hint`, and `error` (turns red, replaces hint).
