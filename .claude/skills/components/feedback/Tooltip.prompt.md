One-line: Basalt tooltip on hover/focus — wrap a single trigger (icon button, label).

```jsx
<Tooltip label="Locked by gate 3"><IconButton label="Info"><Info size={18}/></IconButton></Tooltip>
```

`placement`: top (default) / bottom / left / right.
